steps:
  - group: "Test Group"
    key: "initial-group"
    steps:
      - label: "Initial step"
        command: "echo 'Running initial step'"
      
      - label: "Dynamic upload"
        command: |
          echo "Checking some condition..."
          # Simulate checking a result
          if [ true ]; then
            echo "Condition met, uploading more steps"
            buildkite-agent pipeline upload .buildkite/dynamic-steps.yml
          fi
